import React from "react";
const AudioSubmissionConfirmation = props => (
  <p>
    Are you sure this is the correct location? (Remember to enter an address in
    the search box or zoom in to get as precise a location as possible.)
  </p>
);

export default AudioSubmissionConfirmation;
